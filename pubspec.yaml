name: nickys_designs
description: Nicky's Crochet Designs.

publish_to: 'none'

environment:
  sdk: '>=2.5.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_spinkit: ^5.1.0
  google_fonts: ^2.1.1
  provider: ^6.0.1
  firebase_core: ^1.8.0
  cloud_firestore: ^2.5.4
  nanoid: ^1.0.0
  badges: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/images/
